# 🏠 פרויקט דגל: ניתוח מחירי דירות ומומנטום נדל"ן בישראל
### מדריך מלווה מלא — מקורות נתונים + יומן למידה שבועי (Data Analyst & ML Roadmap)

> קודם כל — כל הכבוד על ההחלטה לבנות פרויקט כזה. זה בדיוק סוג הפרויקט ש**עובד**: נושא רלוונטי, נתונים אמיתיים ומלוכלכים (כמו בעולם האמיתי), וכל שכבות הטכנולוגיה שמראיינים בודקים - Git, Python, BI, ML. בואו נעשה את זה נכון, שלב אחר שלב.

---

## חלק 1️⃣ — מקורות נתונים (Data Sourcing)

### 1.1 המקור המרכזי: "מידע נדל"ן" של רשות המסים
🔗 **https://nadlan.taxes.gov.il**

זהו מאגר עסקאות הנדל"ן הרשמי והמפורט ביותר בישראל. הוא מבוסס על מערכת "כרמ"ן" — כל עסקת מכר מקרקעין מדווחת לרשות המסים על פי חוק מיסוי מקרקעין, ולכן הנתונים אמינים ומדויקים (לא הערכות, אלא מחירים שדווחו בפועל).

**איך עובדים איתו:**
- כניסה חופשית, **ללא צורך בהרשמה**.
- מחפשים לפי עיר → שכונה/רחוב, וניתן לסנן טווח תאריכים וטווח מחירים.
- לכל עסקה מקבלים: תאריך עסקה, כתובת, גוש/חלקה, סוג נכס, מספר חדרים, שטח (מ"ר), קומה, שנת בנייה (לעיתים), ומחיר.
- **חשוב לדעת:** אין API רשמי להורדה מרוכזת — זו מערכת חיפוש אינטרנטית. יש להוציא נתונים דרך חיפוש + ייצוא (או, לשלב מתקדם יותר, כלי עזר קהילתי — ראו סעיף 1.5).

### 1.2 מקור משלים: "Gov נדל"ן" — הפורטל הממשלתי המאוחד
🔗 **https://www.nadlan.gov.il**

אתר עשיר יותר ויזואלית שמציג לצד העסקאות גם מידע תכנוני (תב"עות), מגמות מחירים לפי שכונה, ונתוני איכות חיים (בתי ספר, תחבורה וכו'). מצוין בשביל להעשיר את הפרויקט בהקשר — למשל טענה כמו "מחירים עלו X% בשכונה Y" אפשר לבסס גם דרך כאן.

### 1.3 מקור מאקרו: מדד מחירי הדירות של הלמ"ס (CBS)
🔗 **https://www.cbs.gov.il/he/Pages/מדדי-מחירים-באמצעות-API.aspx**

הלמ"ס מפרסם **מדד מחירי דירות דו-חודשי** דרך API רשמי וחינמי. זה בדיוק המקור ל"מומנטום נדל"ן" שהגדרתם כמטרה — טור זמן ארצי/מחוזי שאפשר להצליב עם נתוני העסקאות הפרטניות כדי לבדוק: האם דירה ספציפית התייקרה יותר/פחות מהשוק הכללי?

💡 שימו לב לנקודה טכנית שהלמ"ס עצמו מדגיש: שלושת המדדים האחרונים שמתפרסמים הם **ארעיים** ומתעדכנים בהמשך — כדאי להימנע מלהתבסס על 2-3 החודשים האחרונים בניתוחי מגמה.

### 1.4 פורטל הנתונים הפתוחים הלאומי — data.gov.il
🔗 **https://data.gov.il**

פורטל CKAN עם מעל 1,000 מאגרים ממשרדי ממשלה. לא תמצאו שם את "עסקאות הנדל"ן" עצמן (הן נמצאות במערכות הייעודיות שלמעלה), אבל שווה לחפש שם מאגרים **משלימים** שיעשירו את המודל:
- מדד חברתי-כלכלי של הלמ"ס לפי יישוב/רשות מקומית (פיצ'ר מצוין לחיזוי מחיר!).
- נתוני היתרי בנייה.
- נתוני אוכלוסייה/צפיפות לפי יישוב.

### 1.5 אופציה מתקדמת (לא חובה): כלי עזר קהילתי לשליפה אוטומטית
🔗 **https://github.com/jmpfar/gov-nadlan-fetcher**

ריפו קוד פתוח שעוטף את ה-API הפנימי של nadlan.gov.il לשליפת עסקאות בצורה אוטומטית. **אזהרה חשובה:** יש הגבלת קצב (rate limiting) אגרסיבית באתר — אם תבחרו בדרך הזו, יש להריץ לאט, עם השהיות, ולא "לשטוף" אלפי בקשות ברצף (גם כדי לא לחסום את ה-IP, וגם מתוך כבוד לשירות ציבורי חינמי).
לצורך פרויקט פורטפוליו, **מומלץ להתחיל בייצוא ידני/חיפוש ממוקד** — זה גם מספיק וגם נמנע מסיבוכים טכניים מיותרים בשלב מוקדם.

### 📊 כמה נתונים להוריד בפועל? (כדי שהפרויקט ירגיש אמיתי בלי למוטט את המחשב)

| פרמטר | המלצה |
|---|---|
| **ערים/יישובים** | 2-4 ערים מגוונות — למשל תל אביב, חיפה, באר שבע, ומודיעין. הגיוון הזה ("שוק חם" מול "שוק פריפריאלי") הוא בדיוק מה שהופך את הניתוח למעניין. |
| **טווח תאריכים** | 3-5 שנים אחורה. מספיק כדי לראות מגמה/מומנטום, לא כל כך הרבה שנתקל בשינויי מטבע/רגולציה ישנים שיסבכו את הניתוח. |
| **מספר שורות (עסקאות)** | **5,000–20,000 שורות** הוא טווח אידיאלי: מרגיש "Big-ish Data" אמיתי, אבל Pandas מתמודד איתו בקלות מוחלטת בזיכרון רגיל. |
| **עמודות** | 10-15 עמודות: תאריך עסקה, עיר, שכונה/רחוב, סוג נכס, חדרים, שטח במ"ר, קומה, שנת בנייה, מחיר, (ואם יש) גוש/חלקה. |
| **קובץ פלט** | שמרו את הגלמי (raw) בנפרד מהמעובד (processed) — תרגלו את זה כבר משלב 0. |

---

## חלק 2️⃣ — יומן למידה ותוכנית עבודה (10 אבני דרך)

> מבנה מוצע: כ-10 שבועות, קצב של פגישת עבודה אחת-שתיים בשבוע. אפשר תמיד להאט/להאיץ — זה **המסגרת שלכם**, לא מרוץ.

---

### 🧱 אבן דרך 0 — תשתית וסביבת עבודה
**שבוע 1 | המטרה:** להקים "בית" מסודר לפרויקט לפני שנוגעים בנתון אחד.

**כלים:** Git, GitHub, VS Code, Python venv/conda, Jupyter

**משימות (Checklist):**
- [ ] להתקין VS Code + תוסף Python + תוסף Jupyter.
- [ ] ליצור סביבה וירטואלית ייעודית (`python -m venv venv` או conda).
- [ ] `git init`, יצירת repository ריק ב-GitHub וחיבור (`git remote add origin ...`).
- [ ] לבנות מבנה תיקיות סטנדרטי:
  ```
  real-estate-israel/
  ├── data/
  │   ├── raw/          ← נתונים גולמיים, לא נוגעים בהם!
  │   └── processed/    ← נתונים אחרי ניקוי
  ├── notebooks/
  ├── src/
  ├── powerbi/
  ├── reports/
  ├── .gitignore
  └── README.md
  ```
- [ ] ליצור `.gitignore` שכולל לפחות: `venv/`, `__pycache__/`, `.ipynb_checkpoints/`, `*.pyc`, קבצי `data/raw/*` גדולים אם צריך.
- [ ] קומיט ראשון: `git commit -m "Initial project structure"` ו-push.
- [ ] לכתוב README ראשוני (שלד) — שם הפרויקט, מטרה במשפט אחד, תוכן עניינים.

**תוצר בסוף השלב:** Repository חי ב-GitHub עם מבנה תיקיות, `.gitignore`, וקומיט ראשון.

**טיפים מהשטח:**
- 🔸 אל תשמרו קבצי דאטה כבדים (מעל כמה עשרות MB) ישירות ב-Git — זה יאט את הריפו לתמיד. אם צריך, תעדו ב-README **איך** להוריד את הדאטה, במקום להעלות אותו.
- 🔸 קבעו מראש מוסכמת שמות לענפים (branches): למשל `feature/data-cleaning`, `feature/powerbi-dashboard`. זה נראה מקצועי מאוד בפורטפוליו.

---

### 📥 אבן דרך 1 — איסוף הנתונים
**שבוע 1-2 | המטרה:** להשיג דאטה אמיתי, בהיקף מבוקר, מהמקורות שמופו למעלה.

**כלים:** דפדפן + חיפוש ידני, Pandas (`read_csv`/`read_excel`), Requests (אופציונלי)

**משימות:**
- [ ] לבחור 2-4 ערים (ראו טבלת ההמלצות למעלה) ולחפש עסקאות ב-nadlan.taxes.gov.il.
- [ ] לייצא/להעתיק את התוצאות לקבצי CSV נפרדים לפי עיר בתוך `data/raw/`.
- [ ] למשוך גם את מדד מחירי הדירות של הלמ"ס (טור זמן ארצי) — זה ה"מומנטום" שישמש להשוואה.
- [ ] לתעד ב-README **בדיוק** מאיפה הגיע כל קובץ ובאילו פרמטרי חיפוש (תאריך + פרמטרים) — קריטי לשחזוריות (reproducibility).
- [ ] קומיט: `git commit -m "Add raw real estate data from tax authority + CBS index"`.

**תוצר בסוף השלב:** תיקיית `data/raw/` עם 2-4 קבצי CSV של עסקאות + קובץ מדד הלמ"ס.

**טיפים מהשטח:**
- 🔸 **קידוד עברית!** קבצים שיוצאים ממערכות ממשלתיות ישראליות לפעמים שמורים ב-CP1255 (Windows-1255) ולא UTF-8. אם `pd.read_csv()` זורק שגיאת encoding או מציג "ג'יבריש", נסו `encoding='cp1255'` או `encoding='iso-8859-8'`.
- 🔸 שמרו עותק "מקור" (raw) שלעולם לא נערך — תמיד תרצו לחזור אליו אם משהו משתבש בניקוי.

---

### 🧹 אבן דרך 2 — ניקוי נתונים וגילוי ראשוני (EDA)
**שבוע 2-3 | המטרה:** להפוך דאטה מלוכלך לטבלה אמינה שאפשר לנתח.

**כלים:** Pandas, NumPy

**משימות:**
- [ ] טעינת כל קבצי הערים ואיחוד ל-DataFrame אחד (`pd.concat`) עם עמודת "עיר".
- [ ] בדיקת `.info()`, `.describe()`, `.isnull().sum()` — הכרות ראשונית.
- [ ] טיפול בערכים חסרים (להחליט: השלמה/הסרה/סימון — ולתעד למה).
- [ ] המרת טיפוסי נתונים: תאריכים ל-`datetime`, מחירים ושטחים למספרים (הסרת פסיקים, סימני ₪ וכו').
- [ ] זיהוי וטיפול ב-**duplicates**.
- [ ] יצירת עמודה מחושבת בסיסית: מחיר למ"ר (`price / area`) — פיצ'ר מרכזי לניתוח.
- [ ] שמירת הדאטה הנקי ל-`data/processed/clean_transactions.csv`.
- [ ] קומיט + push.

**תוצר בסוף השלב:** `notebooks/01_data_cleaning.ipynb` + קובץ CSV נקי.

**טיפים מהשטח:**
- 🔸 **ערכי קיצון (Outliers) הם לב העניין בנדל"ן**: תמצאו רשומות כמו "דירת יוקרה ב-50 מיליון ₪" לצד "עסקת יחסים משפחתיים ב-1 ₪" (עסקאות שאינן ממחיר שוק). אל תמחקו אוטומטית — תבדקו כל קצה, ותשקלו קריטריון כמו IQR או תיעוד "עסקה חריגה" כדגל (flag) ולא מחיקה מיידית. זה בדיוק סוג התובנה שמרשימה בראיון עבודה.
- 🔸 שמרו "יומן ניקוי" (Data Cleaning Log) — כמה שורות הוסרו ולמה, בכל שלב. שקיפות = מקצועיות.

---

### 📊 אבן דרך 3 — ניתוח מעמיק וויזואליזציה
**שבוע 3-4 | המטרה:** לספר את "הסיפור" של הדאטה — מגמות, השוואות, קורלציות.

**כלים:** Pandas (`groupby`, `pivot_table`), Seaborn, Matplotlib

**משימות:**
- [ ] מגמת מחירים לאורך זמן לפי עיר (`groupby(['city','year']).mean()` → line plot).
- [ ] התפלגות מחירים למ"ר לפי עיר (Boxplot / Violin plot) — השוואה ויזואלית חדה.
- [ ] Heatmap של קורלציות בין משתנים מספריים (שטח, חדרים, קומה, שנת בנייה, מחיר).
- [ ] Scatter plot: שטח מול מחיר, עם צביעה (`hue`) לפי עיר.
- [ ] חישוב "מומנטום": שינוי אחוזי במחיר ממוצע רבעון-על-רבעון, בהשוואה למדד הלמ"ס.
- [ ] כל גרף — כותרת, צירים מתויגים בעברית/אנגלית ברור, ומשפט מסקנה תחתיו ב-Markdown cell.
- [ ] קומיט.

**תוצר בסוף השלב:** `notebooks/02_exploratory_analysis.ipynb` עם 6-10 ויזואליזציות + מסקנות כתובות.

**טיפים מהשטח:**
- 🔸 עברית בגרפים לפעמים "נשברת" (מוצגת הפוך) ב-Matplotlib. אם קורה — פתרון מהיר הוא להשתמש בתוויות באנגלית לצירים/כותרות, או לבדוק ספריית `python-bidi`/`arabic_reshaper` (עוזרת גם לעברית לרוב).
- 🔸 אל תסתפקו בגרף בלי מסקנה. כל ויזואליזציה צריכה "אז מה?" — זה מה שהופך Notebook לניתוח, לא לתערוכת גרפים.

---

### 🛠️ אבן דרך 4 — הנדסת פיצ'רים (Feature Engineering)
**שבוע 4-5 | המטרה:** להכין את הדאטה שיהיה "אוכל טוב" למודלים.

**כלים:** Pandas, NumPy, Scikit-Learn (`OneHotEncoder`, `StandardScaler`)

**משימות:**
- [ ] יצירת פיצ'ר "גיל הנכס" (שנה נוכחית פחות שנת בנייה).
- [ ] קידוד משתנים קטגוריאליים (עיר, סוג נכס) — One-Hot Encoding.
- [ ] נרמול/סטנדרטיזציה של משתנים מספריים במידת הצורך.
- [ ] פיצול לפיצ'רים (X) ומשתנה מטרה (y = מחיר).
- [ ] `train_test_split` (למשל 80/20), עם `random_state` קבוע לשחזוריות.
- [ ] שמירת סטים מוכנים (`X_train.csv` וכו') או pipeline מתועד.
- [ ] קומיט.

**תוצר בסוף השלב:** `notebooks/03_feature_engineering.ipynb` + סטי train/test מוכנים.

**טיפים מהשטח:**
- 🔸 **Data Leakage** הוא המלכודת הכי נפוצה כאן: לוודא שה-`train_test_split` קורה **לפני** כל התאמת scaler/encoder (fit רק על train, transform על שניהם).
- 🔸 עמודות עם יותר מדי קטגוריות (למשל "רחוב" עם מאות ערכים ייחודיים) עלולות לפוצץ את המימד ב-One-Hot. שקלו לקבץ לפי שכונה/אזור סטטיסטי במקום רחוב בודד.

---

### 📈 אבן דרך 5 — דשבורד Power BI
**שבוע 5-6 | המטרה:** שכבת הצגה ניהולית — זה מה שגורם למגייסי כוח אדם לעצור ולהסתכל.

**כלים:** Power BI Desktop, Power Query, DAX

**משימות:**
- [ ] ייבוא הדאטה הנקי (`clean_transactions.csv`) ל-Power BI.
- [ ] בניית **Data Model** בסיסי: טבלת עסקאות + טבלת תאריכים (Date table) + (אופציונלי) טבלת ערים עם מטא-דאטה.
- [ ] יצירת מדדי DAX בסיסיים: `Total Transactions`, `Avg Price`, `Avg Price per SQM`.
- [ ] מדד DAX בינוני: **Year-over-Year % Change** במחיר ממוצע (`CALCULATE` + `SAMEPERIODLASTYEAR`) — זה בדיוק "מומנטום הנדל"ן" שלכם, מתורגם לשפת BI.
- [ ] עמוד דשבורד ראשי: כרטיסי KPI + מפת חום/עמודות לפי עיר + גרף מגמה לאורך זמן.
- [ ] הוספת **Slicers** (עיר, טווח תאריכים, סוג נכס) לאינטראקטיביות.
- [ ] שמירה כ-`powerbi/real_estate_dashboard.pbix`.
- [ ] קומיט (הקובץ עצמו כבינארי — לתעד בREADME גרסת Power BI וצילומי מסך).

**תוצר בסוף השלב:** קובץ `.pbix` עם דשבורד אינטראקטיבי + צילומי מסך ב-`reports/`.

**טיפים מהשטח:**
- 🔸 קובצי `.pbix` הם בינאריים — Git לא "יודע" להראות diff שימושי. תוסיפו לפחות 2-3 צילומי מסך של הדשבורד ל-README, כי לא כולם יפתחו את הקובץ.
- 🔸 מדד DAX כמו YoY% נשבר בקלות אם טבלת התאריכים לא "רציפה" (חסרים ימים/חודשים) — ודאו שה-Date table מכסה את כל הטווח ברצף, גם אם אין בו עסקאות בכל יום.

---

### 🤖 אבן דרך 6 — מודל בסיס: Linear Regression
**שבוע 6-7 | המטרה:** קו בסיס (Baseline) פשוט וניתן להסבר — נקודת ייחוס לכל מה שיבוא אחריו.

**כלים:** Scikit-Learn (`LinearRegression`), `metrics` (RMSE, R²)

**משימות:**
- [ ] אימון `LinearRegression` על סט האימון.
- [ ] חיזוי על סט הטסט וחישוב **RMSE** ו-**R²**.
- [ ] בדיקת שאריות (Residuals) — האם יש דפוס (סימן לבעיה במודל) או רעש אקראי (טוב)?
- [ ] פרשנות מקדמים (Coefficients) — אילו פיצ'רים משפיעים הכי הרבה על המחיר, ובאיזה כיוון?
- [ ] תיעוד תוצאות בטבלה שתשמש להשוואה בהמשך.
- [ ] קומיט.

**תוצר בסוף השלב:** `notebooks/04_baseline_model.ipynb` עם מודל, מדדים, וגרף שאריות.

**טיפים מהשטח:**
- 🔸 R² נמוך זה **לא כישלון** — זו בדיוק הסיבה שממשיכים ל-Random Forest/XGBoost בשלב הבא. תעדו את זה ככה: "המודל הליניארי קובע Baseline של R²=X — נבדוק אם מודלים לא-ליניאריים משפרים."
- 🔸 שימו לב ל-**מולטיקולינאריות** (למשל שטח וחדרים מתואמים ביניהם חזק) — זה יכול לעוות את פרשנות המקדמים גם אם החיזוי עצמו תקין.

---

### 🌲 אבן דרך 7 — בנצ'מרק מתקדם: Random Forest / XGBoost
**שבוע 7-8 | המטרה:** לבדוק אם מודלים מורכבים יותר "תופסים" דפוסים לא-ליניאריים בשוק.

**כלים:** Scikit-Learn (`RandomForestRegressor`), XGBoost (`XGBRegressor`)

**משימות:**
- [ ] אימון `RandomForestRegressor` עם פרמטרים בסיסיים.
- [ ] אימון `XGBRegressor`.
- [ ] Cross-Validation (למשל `KFold` 5) לכל מודל, לא רק split בודד — כדי לקבל תמונה יציבה יותר.
- [ ] טבלת השוואה מסודרת: Linear Regression vs Random Forest vs XGBoost, לפי RMSE ו-R² (train ו-test, כדי לזהות Overfitting).
- [ ] כיוונון בסיסי של היפרפרמטרים (`n_estimators`, `max_depth`) — לא חובה Grid Search מלא, אבל כמה ניסיונות מתועדים.
- [ ] קומיט.

**תוצר בסוף השלב:** `notebooks/05_advanced_models.ipynb` + טבלת השוואה סופית.

**טיפים מהשטח:**
- 🔸 **Overfitting הוא הסיכון המרכזי כאן**: אם RMSE על ה-train נמוך בהרבה מאשר על ה-test, המודל "שינן" ולא למד. פתרון ראשוני: להגביל `max_depth`, להוסיף `min_samples_leaf`.
- 🔸 אל תשוו מודלים על מדד יחיד — RMSE טוב אך R² חלש (או להפך) מספר סיפור שונה. הציגו את שניהם תמיד יחד.

---

### 🔍 אבן דרך 8 — הסבר המודל (Explainability)
**שבוע 8-9 | המטרה:** לא מספיק לחזות — צריך להסביר **למה** המודל מחליט מה שהוא מחליט. זה מה שמבדיל פרויקט Data Science בוגר מ"עוד מחברת שהריצה `.fit()`".

**כלים:** Scikit-Learn (`feature_importances_`), SHAP (מומלץ, לא חובה)

**משימות:**
- [ ] הפקת גרף **Feature Importance** מהמודל המנצח (RF/XGBoost).
- [ ] (מומלץ מאוד) התקנת `shap` והפקת SHAP summary plot — מראה לא רק *מה* חשוב, אלא *איך* כל פיצ'ר מושך את המחיר למעלה/למטה.
- [ ] כתיבת פסקת מסקנות: "הפיצ'רים המשפיעים ביותר על מחיר דירה בישראל, לפי המודל, הם..." — עם הסבר עסקי, לא רק טכני.
- [ ] השוואה בין תובנות המודל לבין מה שראיתם ב-EDA (שלב 3) — האם הן מתיישבות?
- [ ] קומיט.

**תוצר בסוף השלב:** `notebooks/06_model_explainability.ipynb` + פסקת מסקנות עסקיות.

**טיפים מהשטח:**
- 🔸 SHAP יכול להיות איטי על דאטה סטים גדולים — עם 5,000-20,000 שורות (כמו שהמלצנו) זה יעבוד חלק לגמרי.
- 🔸 היזהרו מבלבול בין **קורלציה לסיבתיות**: "קומה גבוהה מנבאת מחיר גבוה" זה תובנת מודל תקינה, אבל לא בהכרח "קומה גבוהה **גורמת** למחיר גבוה" (יכול להיות שדירות יוקרה בכלל נבנות בקומות גבוהות).

---

### 🎓 אבן דרך 9 — גימור, תיעוד והצגה בפורטפוליו
**שבוע 9-10 | המטרה:** לארוז הכל בצורה שגורמת למראיין/לצופה להבין תוך 30 שניות שזה פרויקט רציני.

**כלים:** Git/GitHub (README, branches, tags), Markdown

**משימות:**
- [ ] כתיבת README.md סופי הכולל: כותרת + תקציר של שורה-שתיים, מטרת הפרויקט, מקורות הנתונים (עם קרדיט), ארכיטקטורת הפרויקט (תרשים תיקיות), הטכנולוגיות, **תוצאות עיקריות** (2-3 תובנות + טבלת השוואת מודלים), צילומי מסך מהדשבורד, הוראות הרצה.
- [ ] כתיבת README גם בעברית **וגם** באנגלית (או README דו-לשוני) — פותח דלת למגייסים בינלאומיים.
- [ ] בדיקה שכל ה-branches מוזגו נקי ל-`main`, ותיוג גרסה (`git tag v1.0`).
- [ ] ניקוי notebooks: להריץ מחדש מההתחלה (Restart & Run All) לוודא שהכל רץ בלי שגיאות, ולנקות outputs מיותרים/ארוכים מדי.
- [ ] (בונוס) הוספת קובץ `requirements.txt` או `environment.yml` לשחזוריות מלאה.
- [ ] שיתוף הפרויקט — לינקדאין / קורות חיים / תיק עבודות אישי.

**תוצר בסוף השלב:** Repository מלוטש, מתועד, ומוכן להצגה — **פרויקט הדגל שלכם**.

**טיפים מהשטח:**
- 🔸 README הוא לרוב **הדבר היחיד** שמראיין קורא לפני שהוא מחליט אם לפתוח בכלל את הקוד. תשקיעו בו יותר משתחשבו שצריך.
- 🔸 אם יש זמן — סרטון קצר (2-3 דקות) שמדגים את דשבורד ה-Power BI "חי" עושה רושם עצום ועוקף את הבעיה שרוב הצופים לא יתקינו Power BI כדי לפתוח את הקובץ בעצמם.

---

## 🧭 סיכום מהיר — טבלת התקדמות

| # | אבן דרך | תוצר מרכזי |
|---|---|---|
| 0 | תשתית | Repo מוכן ב-GitHub |
| 1 | איסוף נתונים | קבצי CSV גולמיים |
| 2 | ניקוי + EDA ראשוני | דאטה נקי |
| 3 | ניתוח וויזואליזציה | Notebook עם תובנות |
| 4 | הנדסת פיצ'רים | סטי train/test |
| 5 | דשבורד Power BI | קובץ .pbix |
| 6 | Baseline Model | Linear Regression |
| 7 | מודלים מתקדמים | RF/XGBoost + השוואה |
| 8 | הסבר מודל | SHAP + מסקנות |
| 9 | גימור והצגה | README מלוטש + פרויקט מוכן |

בהצלחה גדולה לשניכם — ותהנו מהתהליך, לא רק מהתוצאה! 💪
